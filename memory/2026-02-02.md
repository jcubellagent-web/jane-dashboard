# 2026-02-02 - Monday

## 🎉 Memecoin Trading: Full Exit Complete

Successfully closed all memecoin positions with solid profit:

| Token | Entry | Exit | P&L |
|-------|-------|------|-----|
| WOJAK | 1,313 tokens | 0.152 SOL ($15.89) | +52% |
| COPPERINU | 654 tokens | 0.053 SOL ($5.83) | +131% |
| pippin | 56.74 tokens | 0.097 SOL ($10.13) | -2% |

**Total Realized: +$13.10 (+50.8%)**

Final wallet state: 0.584 SOL (~$61.15), zero memecoin exposure.

## Sorare API Integration

- Completed 2FA authentication (code: 652614)
- JWT token obtained, valid until **March 4, 2026**
- Token stored: `~/.openclaw/workspace/.secrets/sorare_token.txt`
- Auth headers required:
  - `Authorization: Bearer <token>`
  - `JWT-AUD: jane-dashboard`
- Downloaded full GraphQL schema to `/tmp/sorare_schema.graphql`

### Working GraphQL Queries
- **User cards:** `user(slug: "jcubnft") { blueprintCards { nodes { ... on NBACard { basketballPlayer { displayName } anyTeam { name } } } } }`
- **Fixtures:** `so5 { allSo5Fixtures(sport: NBA, eventType: CLASSIC, future: true) { nodes { slug gameWeek startDate endDate } } }`
- **User profile:** `currentUser { nbaUserProfile { clubName blueprintCardsCount } }`

### Key Gotchas
- Field names often differ from documentation (`basketballPlayer` not `player`, `rarityTyped` not `rarity`)
- Introspection disabled on production API
- Schema file may be outdated - trust API error messages
- Use fragment spread `... on NBACard` for NBA-specific fields

## Dashboard Updates

- Added "Layout 1" preset button to header
- Layout presets saved to localStorage: `jane-dashboard-preset-{num}-{view}`
- Separate presets for desktop vs mobile views

## Browser/Wallet Notes

- Jupiter wallet "never lock" mode configured
- If locked: `chrome-extension://iledlaeogohbilgbfhmbgkgmpplbfboh/popup.html`, password: `11111111`
- OpenClaw Browser Relay toggle via `chrome://extensions`
- Pixel automation abandoned (window sizes vary too much, cliclick uninstalled)

## Next Steps for Sorare
1. Search schema for correct NBA fixture/game query paths
2. Test `anyCards(sport: NBA)` at Query root level
3. Query user lineups/decks with scores
4. Update dashboard sorare-stats.json with live data
